const assert = require('chai').assert;

const MelodyUtils = require('../lib/utils');

describe('MelodyUtils', function() {
  it('should calculate the length of a beat', function(done) {
    let 
    assert.equal()
  });
});